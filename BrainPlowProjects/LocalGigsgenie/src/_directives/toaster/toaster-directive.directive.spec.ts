import { ToasterDirectiveDirective } from './toaster-directive.directive';

describe('ToasterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ToasterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
